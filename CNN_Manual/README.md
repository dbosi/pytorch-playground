# Manual CNN

A CNN built with pytorch functions, batches, train/val split and weights initialized with Kaiming initialization.

Manual convolve function implemented but the implementation is too slow, so pytorch convolve function used.

Dataset used: MNIST Fashion

Accuracy achieved: 88%